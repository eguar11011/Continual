dataset: cifar10          # Datas­­et base
classes-per-task: 5       # 5 clases por tarea (2 tareas en CIFAR-10)
strategy: replay          # ← estrategia
backbone: resnet18
epochs: 30
batch: 64
img-size: 224

# hiperparámetros propios de Replay
buffer: 2000              # tamaño del buffer de memoria (ejemplo)
